{
    "config": {
        "allow-plugins": {
            "symfony/flex": true,
            "symfony/runtime": true
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "require": {
        "symfony/webapp-pack": "^1.3"
    }
}
